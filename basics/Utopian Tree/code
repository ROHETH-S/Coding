public static int utopianTree(int n) {
        
        int ans=1;
        for(int i=1;i<=n;i++){
            if(i%2!=0) ans*=2;
            else ans++;
            
        }
        return ans;
    }
