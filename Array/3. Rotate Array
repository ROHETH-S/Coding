class Solution {
public:
    void rotate(vector<int>& nums, int k) 
    {
        int l=nums.size();
        vector<int> temp(l);
   
        for(int i=0; i<l; i++)
        {
            temp[(i+k)%l]=nums[i];
        }
        
        nums=temp;
    }
};
