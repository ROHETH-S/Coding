segmentation fault occurse if there is no return value or return value is of other type

run time error happens, when size is not declared properly 
 vector<int> ans(max_num+1, 0); do like this


for reversing the digit, it is better to convdert as string and do reverse operation
number can be converted to string by "to_string()"
and string can be converted  to int by "stoi()" or by subtracting (str[i] - '0')


int max_elem1= *max_element(res.begin(), res.end());
             auto x = find(res.begin(), res.end(), max_elem1);
             if (x != res.end()) res.erase(x);
                   
int min_elem1=*min_element(a.begin(), a.end());

  for (int num : unique1) 
        {
            if (unique2.count(num) == 0) 
            {
                res1.push_back(num);
            }
        }
       count function used to find the presence the of element unique1 set in unique2 set

iterating in reverse make sure that
   for(int i=str.size()-1; i>=0; i--)

if you are doing operation with number present in string then make sure that(-'0')
for(int i=0; i<str[i] - '0'; i++)
