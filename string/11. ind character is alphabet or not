#include<bits/stdc++.h>
using namespace std;

int removespace(char c)
{
    if(isalpha(c))
    {
        cout<<" it is an alphabet";
    }
    
    else
    {
        cout<<" not an alphabet";
    }

    
    
    return 0;
   
}

int main()
{
    char c;
    cin>>c;
    removespace(c);
}
